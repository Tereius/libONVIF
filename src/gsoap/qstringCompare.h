#pragma once
#include <QString>

bool operator>(const QString &s1, int length);
bool operator>=(const QString &s1, int length);
bool operator<(const QString &s1, int length);
bool operator<=(const QString &s1, int length);